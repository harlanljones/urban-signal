# Stream: US-437 — Expand Division & Submarket Registry to Cover All 9 Counties

**Ticket:** US-437  
**Status:** Done  
**Agent:** Antigravity (Gemini 2.5 Pro → Claude Sonnet 4.6)  
**Base branch:** main (after US-436 landed)

## Scope

Leaf stream — no spine edits other than updating the SF spatial module files (which are the designated leaf for this ticket). Dashboard CSS/JS normalization changes are additive-only (new division name cases, no structural changes).

## Changes made

| File | Type | Description |
|---|---|---|
| `apps/api/src/spatial/cities/san_francisco.py` | core | 5→8 divisions, 41→83 submarkets |
| `apps/api/src/spatial/cities/data/san_francisco.yaml` | derived | regenerated from py |
| `apps/api/src/serving/dashboard.py` | additive | CSS + JS for 3 new divisions |
| `apps/api/tests/unit/test_submarkets.py` | test | counts, division assertions, disambiguation |
| `apps/api/tests/unit/test_serving.py` | test | count assertions |
| `apps/product/public/cities/san_francisco.json` | artifact | regenerated via facts:export |
| `apps/dashboard/public/index.html` | artifact | regenerated via export_dashboard.py |

## New divisions

- `NORTH_BAY_WINE_COUNTRY`: Napa + Sonoma (7 submarkets)
- `SOLANO_CORRIDOR`: Solano County (6 submarkets)
- `OUTER_CONTRA_COSTA`: Central/East CCC (9 submarkets)

## Gates

- `pytest -m interlock`: 35/35 ✅
- `python3 scripts/verify_cicd_preflight.py`: all 6 gates ✅

## Pre-existing issues noted

- `test_get_city_for_coordinate` with Oakland Downtown was already failing on main before US-437; updated to assert `"oakland"` (documented in US-436 stream).
- Rockridge in both Oakland and SF registries: disambiguated with `city_id="san_francisco"` in tests.
