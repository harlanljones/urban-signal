# Stream: us-429-430-428 registry derivation finish + field-map consolidation

## Claim

- Stream id: `us-429-430-428`
- Tickets: US-429 (done — gather + CITY_EXPORT_NAMES derived), US-430 (field maps
  into leaves), US-428 (registry derivation finish). US-430 and US-428 land as
  one coordinated migration per their own sequencing notes.
- Leaf files: `apps/api/src/spatial/cities/<city>.py` (~70 leaves inlining
  field maps), `apps/api/src/spatial/cities/data/*.yaml` (corpus completion),
  `apps/api/src/producers/field_maps.py` (resolver stays), producer spec
  helper modules, `apps/api/tests/unit/test_producers_*.py` rewires,
  `apps/api/tests/unit/test_derived_registry.py` rewrite.
- Spine files needed: `apps/api/src/spatial/city_registry.py` (delete
  `_HANDWRITTEN_REGISTRY`/`_HANDWRITTEN_ALIASES`, single construction path).
  `cities/__init__.py` NOT touched again (derived since US-429).

## Decisions

- Corpus regeneration via `scripts/export_city_data.py` is the migration
  vehicle: the handwritten registry is authoritative today, the YAML corpus is
  inert (build_runtime_exports returns None while incomplete). Re-exporting
  all cities from handwritten guarantees the derived path is equivalent by
  construction, and inlines every field map as data.
- Post-migration: `registry_derivation.build_runtime_exports` is the ONLY
  construction path (corpus data + leaf REGISTRATION geometry, loud failure on
  missing corpus entries); `_HANDWRITTEN_*` deleted; `USE_DERIVED_REGISTRY`
  flag removed; `scripts/export_city_data.py` retired.
- field_maps_<city>.py contents merge into the owning leaf
  `cities/<city>.py`; importer rewrites point at the leaf. Special modules:
  `field_maps_tx_trec` → `tx_trec_specs.py`, `field_maps_state_licenses` →
  `state_license_specs.py`. `field_maps.py` (resolver) stays.

## Status

- [x] US-429 done (resolved on Linear with evidence). Derived gather in
      `cities/__init__.py`; `CITY_EXPORT_NAMES` derived from `CityId → module`.
- [x] US-428 done (resolved on Linear with evidence). `registry_derivation.py`
      is the only construction path; `_HANDWRITTEN_REGISTRY`/`ALIASES` and
      `USE_DERIVED_REGISTRY` deleted (city_registry 8,767 → 421 lines); corpus
      regenerated for all 132 cities; `city_data_dir` cwd bug fixed; feedless
      registration allowed; `verify-site-content.mjs` reads the CityId enum.
- [x] Post-flip test harness updated for corpus-derived value-equality semantics
      (dallas, snap, portland, la, oxnard_ventura, etc.: `.field_map is` → `==`).
- [x] Fixed corpus drift: portland SLA spec carried the stale pre-US-372 OLCC
      endpoint/field-map; corrected to the leaf's authoritative CCB spec.
- [~] US-430 attempted but NOT completed. The physical `field_maps_<city>.py`
      deletion is a large (82 modules / ~142 importers) refactor with
      cross-module deps (state_licenses, tx_trec, feed-family helpers) and
      function-bearing modules (e.g. `normalize_laredo_row`). It also edits the
      same leaves the concurrent stream is reshaping. Attempted a blanket
      scripted inline+rewire; reverted it cleanly to keep the tree green rather
      than ship a partial torn state. US-430's data-intent (maps as leaf
      DatasetSpec data) is already satisfied by US-428's corpus; only the
      physical module deletion + importer rewiring remains, best done per-city
      in batches per the ticket's own sequencing note.
- [x] Interlock green (25); full CI/CD preflight green after each hold.

## Concurrent-stream note

The parallel stream was actively onboarding cities (peoria, new_haven,
bridgeport, worcester, laredo + others) during this work. Its leaves were
repaired (field-map constants defined after use → reordered) so the gather
imported them again. `city_registry.py`, `city_data.py`, `config.py`,
`registry_derivation.py`, and `cities/__init__.py` are the spine files changed
here and verified green.

## Concurrent-stream note

A parallel stream is onboarding new cities in this tree (peoria — spine entry
staged; new_haven — leaf landed, spine entry pending). Before the spine hold,
re-check `git diff` on `city_registry.py` for their edits and include them in
the corpus regeneration.
