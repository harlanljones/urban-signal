# Stream log — us-436 — 2026-09-06

## Claim

- **Stream id:** us-436
- **Leaf files I will create/edit:**
  - `apps/api/src/spatial/bay_area_boundary.py` (new: 9-county boundary loader + res-5 polyfill + water mask)
  - `apps/api/src/spatial/assets/sf_bay_area_9county.geojson` (new: dissolved multipolygon)
  - `apps/api/src/spatial/assets/sf_bay_area_9county_water.geojson` (new: bay/ocean water mask)
  - `apps/api/src/spatial/assets/sf_bay_area_counties.geojson` (new: 9 per-county polygons for coverage test)
  - `apps/api/tests/unit/test_bay_area_boundary.py` (new: coverage/determinism tests)
  - `apps/api/src/spatial/cities/data/san_francisco.yaml` (metro_bbox 4 lines only — mirror of leaf)
  - `apps/product/public/cities/san_francisco.json` + `facts.json` (via `bun run facts:export`)
  - `apps/dashboard/public/index.html` (via `python3 scripts/export_dashboard.py` if viewport bytes change)
- **Spine files I expect to need:**
  - `apps/api/src/spatial/cities/san_francisco.py` (SF_METRO_BBOX 4 lines only — additive expansion, divisions stay nested)

## Intent

Done = canonical 9-county GeoJSON committed, deterministic res-5 polyfill (~85 tiles) with
is_water flags, SF_METRO_BBOX on the full 9-county envelope, coverage test green,
`pytest -m interlock` green, preflight green. No scope creep into US-437..US-445
(divisions/submarkets/tiles wiring stay as-is).

## Decisions

- 2026-09-06 — Claimed in Linear: `issue update US-436 -a self -s "In Progress"` OK
  (`issue start` fails with "Could not determine team ID"; update works).
- 2026-09-06 — Boundary source: Census TIGERweb State_County MapServer (TIGER/Line
  county geometry) layer 1, full geometry, 9 CA FIPS. Direct www2.census.gov
  TIGER2024 zip path 404s; TIGERweb GeoJSON query works.
- 2026-09-06 — Water mask: TIGERweb Hydro Areal Hydrography, MTFCC H2051 (bay/estuary)
  + H2053 (ocean/sea) intersecting the envelope — SF Bay system + Pacific + Tomales/
  Drakes/Bodega bays. Inland lakes/reservoirs (H2030/H2040) excluded per ticket scope.
- 2026-09-06 — REGISTRY geometry is re-bound from the leaf .py at runtime
  (registry_derivation.py), so the canonical bbox edit is san_francisco.py; the yaml
  is a 4-line mirror. Divisions unchanged: all nest inside the expanded envelope,
  so containment holds without touching them.
- 2026-09-06 — Expansion is a strict superset (36.89–38.86 / -123.53–-121.20), so
  center-in-metro, division nesting, submarket-in-division invariants are preserved.

## Current step

Done with implementation + verification. Writing PR_DESCRIPTION.md, moving Linear to In Review.

## Verification log

- `tests/unit/test_bay_area_boundary.py`: 10/10 pass (asset validity, count=78,
  determinism, per-county coverage, water=12, scorable exclusion, bbox sync).
- `pytest -m interlock` (apps/api): 35/35 pass — incl. dashboard wiring + snapshot
  wiring with the expanded bbox (metro_cells is submarket-ring based, so the
  snapshot manifest is byte-identical; METRO_META carries names only, so
  `export_dashboard.py` regenerates byte-identical index.html).
- `python3 scripts/verify_cicd_preflight.py`: GREEN all 6 gates (used a gitignored
  `apps/api/.venv/bin/python -> python3` shim since the script hardcodes the venv
  path; shim removed afterward).
- `bun run facts:export` regenerated `apps/product/public/cities/san_francisco.json`
  (new metro_bbox); `facts:check` green.
- Pre-existing failures (identical on clean main 3386ff0, unrelated to US-436):
  `test_submarkets.py::test_get_submarket_by_name_multi_city` and
  `::test_get_city_for_coordinate` — Oakland metro registration overlaps SF's
  downtown-Oakland sample coordinate (`assert 'oakland' == 'san_francisco'`).
  Left untouched (other ticket's scope).

## Next step

Human review + commit. Follow-ups US-437..US-445 consume `bay_area_boundary`
(water-masked res-5 tiles) for snapshot/grid-tile wiring and division expansion.
