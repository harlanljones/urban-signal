# Stream log — us-374 — 2026-09-06

## Claim

- **Stream id:** `us-374`
- **Ticket:** US-374: Build NppesDiffProducer; register NPI registry weekly diffs as medical-office churn
- **Leaf files:**
  - `apps/api/src/producers/nppes_diff_producer.py`
  - `apps/api/tests/unit/test_nppes_diff_producer.py`
- **Spine files:**
  - `apps/api/src/spatial/national_feeds.py`
  - `apps/api/src/producers/scheduler.py`
- **Test files:**
  - `apps/api/tests/unit/test_gbfs_and_national_feeds.py`
  - `apps/api/tests/unit/test_scheduler_national_feeds.py`
  - `scripts/verify_cicd_preflight.py`

## Intent

Implement the NPPES weekly diff producer, register `NationalFeed.NPPES_MEDICAL` to ingest NPI registry weekly diffs as medical-office churn, wire the scheduler job for continuous polling/dispatch, and verify all unit tests and CI/CD preflight gates pass.

## Decisions

- **2026-09-06** — Verified `NppesDiffProducer` leaf implementation. Added `self.socrata = None` and `run_stream(...)` to `NppesDiffProducer` to conform to `MunicipalIngestionScheduler` platform dispatch and interlock gate invariants.
- **2026-09-06** — Registered `NationalFeed.NPPES_MEDICAL` in `apps/api/src/spatial/national_feeds.py` using `topic=settings.topic_sla`, `producer_key="nppes"`, `id_keys=["NPI"]`, `interval_seconds=7 * 86400.0`, `ingestion_mode="diff"`.
- **2026-09-06** — Wired `nppes` into `MunicipalIngestionScheduler.producers` and included `NationalFeed.NPPES_MEDICAL` in national job configurations.
- **2026-09-06** — Updated `test_gbfs_and_national_feeds.py` to recognize all registered national feeds and to exclude the shared `topic_sla` when verifying national feeds don't collide with municipal datasets.
- **2026-09-06** — Added unit test `test_nppes_medical_is_registered_and_dispatchable` in `test_scheduler_national_feeds.py` and unit tests in `test_nppes_diff_producer.py`.
- **2026-09-06** — Updated `scripts/verify_cicd_preflight.py` with `sys.executable` fallback for worktrees without local `.venv`.
- **2026-09-06** — Executed `pytest -m interlock` and `python3 scripts/verify_cicd_preflight.py`; all gates passed cleanly.

## Outcome

- All unit tests and interlock tests passing.
- CI/CD preflight is green.
