# Stream log — us-430 — 2026-09-06

## Claim

- **Stream id:** `us-430`
- **Linear Ticket:** US-430: "Architecture: move per-city field maps into leaf DatasetSpecs (completes ADR-0001 §9.2)"
- **Leaf files created/edited/deleted:**
  - 79 obsolete per-city `apps/api/src/producers/field_maps_<city>.py` deleted.
  - 12 shared & state/national helper field maps retained in `apps/api/src/producers/` (`field_maps.py`, `field_maps_ca_licenses.py`, `field_maps_childcare.py`, `field_maps_co_dora.py`, `field_maps_counters.py`, `field_maps_energy_benchmark.py`, `field_maps_fl_cadastral.py`, `field_maps_fmcsa.py`, `field_maps_michigan_lara.py`, `field_maps_ohio_elicense.py`, `field_maps_state_licenses.py`, `field_maps_tx_trec.py`).
  - Leaf modules updated with needed constants/exports: `apps/api/src/spatial/cities/{austin,boston,el_paso,honolulu,milwaukee,phoenix,portland,san_jose,st_louis}.py`.
  - All producer unit tests in `apps/api/tests/unit/test_producers_*.py` rewired to import from leaf `src.spatial.cities.<city>` instead of `src.producers.field_maps_<city>`.
  - `apps/api/tests/unit/test_asheville_deeds_spec.py` rewired to `src.producers.asheville_deeds_spec`.
  - Producer scheduler unit tests patched to prevent background kafka producer socket creation during unit tests.
- **Spine files:** None modified; spine intact.

## Intent

Complete US-430 (and the final step of ADR-0001 §9.2) by removing all 79 obsolete per-city `field_maps_<city>.py` modules from `src/producers/`, ensuring all field maps are declared directly in leaf specs / YAML corpus definitions (`src/spatial/cities/data/<city_id>.yaml` and `src/spatial/cities/<city_id>.py`), and rewiring all tests and importers cleanly.

## Decisions

- **Retained Non-City / Shared Field Maps:** Retained 12 non-city helpers in `apps/api/src/producers/`:
  - `field_maps.py` (shared resolver and fallback mapping)
  - `field_maps_ca_licenses.py` (California state licensing specs)
  - `field_maps_childcare.py` (shared childcare dataset field map)
  - `field_maps_co_dora.py` (Colorado DORA state licensing specs)
  - `field_maps_counters.py` (shared pedestrian/bike counter field map)
  - `field_maps_energy_benchmark.py` (shared energy benchmark field map)
  - `field_maps_fl_cadastral.py` (Florida state cadastral parcel specs)
  - `field_maps_fmcsa.py` (national FMCSA motor carrier specs)
  - `field_maps_michigan_lara.py` (Michigan LARA state licensing specs)
  - `field_maps_ohio_elicense.py` (Ohio eLicense state licensing specs)
  - `field_maps_state_licenses.py` (multi-state license specs)
  - `field_maps_tx_trec.py` (Texas TREC state licensing specs)
- **Deleted 79 Per-City Field Map Files:** Deleted all per-city `field_maps_<city>.py` files from `apps/api/src/producers/`.
- **Rewired Tests to Leaf Modules:** Updated all 75+ `test_producers_<city>.py` test suites to import field maps, field lists, normalization functions, and constants directly from `src.spatial.cities.<city>`.
- **Circular Import Prevention:** Ensured city leaf modules in `src/spatial/cities/` do not import `FeedType` or `city_registry` directly.
- **Kafka Test Isolation:** In `test_scheduler.py`, `test_scheduler_stagger.py`, and `test_scheduler_watermark_state.py`, wrapped `MunicipalIngestionScheduler` instantiation in `unittest.mock.patch("src.producers.base_producer.BaseKafkaProducer")` to avoid live socket connections to `localhost:9092` during unit tests.
- **CalEnviroScreen Centroid Fallback:** Handled absent `calenviroscreen_tract_centroids.json` gracefully with `_CENTROIDS_PATH.exists()` check and test skip.

## Verification

- `PYTHONPATH=.:../.. .venv/bin/pytest tests/unit/test_producers_*.py`: 3,050 tests passing.
- `python3 scripts/verify_cicd_preflight.py`: All 6 gates passed green:
  1. API interlock gate (`pytest -m interlock`) OK
  2. Dashboard ↔ product-site cross-reference OK
  3. Product facts drift (`bun run facts:check`) OK
  4. Product site lint (`bun run lint`) OK
  5. Dashboard export (`python3 scripts/export_dashboard.py`) OK
  6. Ruff check OK
