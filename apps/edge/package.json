{
  "name": "@urban-signal/edge",
  "version": "2.0.0",
  "private": true,
  "description": "Urban Signal edge worker serving precomputed snapshots from Workers KV",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --outdir dist",
    "test": "bun test tests",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@urban-signal/typescript-config": "workspace:*",
    "@cloudflare/workers-types": "^5.20260823.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.125.0"
  }
}
