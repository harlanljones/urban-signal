{
  "name": "@urban-signal/dashboard",
  "version": "2.0.0",
  "private": true,
  "description": "Urban Signal edge worker serving precomputed snapshots from Workers KV",
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_SEND_METRICS=false wrangler dev",
    "build": "WRANGLER_SEND_METRICS=false WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --outdir dist",
    "test": "bun test tests",
    "deploy": "WRANGLER_SEND_METRICS=false wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@urban-signal/typescript-config": "workspace:*",
    "@cloudflare/workers-types": "^5.20260823.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.125.0"
  }
}
